# Core MintPaper engine dependencies
ewmh==0.1.6              
python-mpv==1.0.8       
python-xlib==0.33       
six==1.17.0             

# System monitoring and interaction
psutil>=5.8.0           
pynput>=1.7.6           

# --- ADDITIONS ---

# Networking / Socket Support (Standard Library, but listed for clarity)
# No pip install needed for 'socket', but ensuring it's not blocked

# JSON Configuration handling
# No pip install needed for 'json', part of Python standard library

# File system watching for the "Hot-Reload" feature
# If you plan on having the engine auto-refresh when you edit an HTML file:
watchdog>=3.0.0

# Optional: Setproctitle for cleaner 'ps' and 'top' output
# This makes the process show as "MintpaperEngine" instead of "python3 main.py"
setproctitle>=1.3.3